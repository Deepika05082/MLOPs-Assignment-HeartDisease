apiVersion: v1
kind: Service
metadata:
  name: heart-disease-app
spec:
  type: NodePort
  selector:
    app: heart-disease-app
  ports:
    - name: http        # required name
      port: 8000        # service port
      targetPort: 8000  # container port
      nodePort: 30080   # optional fixed NodePort
